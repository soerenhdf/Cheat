-- Roblox Hack Menu with GitHub Key System
-- UI Library
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- Key Settings
local validKey = nil
local keyURL = "https://raw.githubusercontent.com/soerenhdf/Key/refs/heads/main/key.txt"
local linkText = "This is the key"
local linkURL = "https://link-target.net/1339083/key"

-- Create Key UI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KeySystem"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 300, 0, 200)
Frame.Position = UDim2.new(0.5, -150, 0.5, -100)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.Name = "KeyFrame"
Frame.Parent = ScreenGui

local Title = Instance.new("TextLabel")
Title.Text = "Enter Key to Access Menu"
Title.Size = UDim2.new(1, 0, 0, 30)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 22
Title.Parent = Frame

local TextBox = Instance.new("TextBox")
TextBox.PlaceholderText = "Enter your key here"
TextBox.Size = UDim2.new(0.8, 0, 0, 40)
TextBox.Position = UDim2.new(0.1, 0, 0.3, 0)
TextBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.ClearTextOnFocus = false
TextBox.Font = Enum.Font.SourceSans
TextBox.TextSize = 18
TextBox.Parent = Frame

local Submit = Instance.new("TextButton")
Submit.Text = "Submit"
Submit.Size = UDim2.new(0.4, 0, 0, 40)
Submit.Position = UDim2.new(0.3, 0, 0.55, 0)
Submit.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
Submit.TextColor3 = Color3.fromRGB(255, 255, 255)
Submit.Font = Enum.Font.SourceSansBold
Submit.TextSize = 20
Submit.Parent = Frame

local LinkButton = Instance.new("TextButton")
LinkButton.Text = linkText
LinkButton.Size = UDim2.new(1, -20, 0, 30)
LinkButton.Position = UDim2.new(0, 10, 0.8, 0)
LinkButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
LinkButton.TextColor3 = Color3.fromRGB(0, 170, 255)
LinkButton.Font = Enum.Font.SourceSansBold
LinkButton.TextSize = 18
LinkButton.Parent = Frame

LinkButton.MouseButton1Click:Connect(function()
    StarterGui:SetCore("OpenUrl", linkURL)
end)

Submit.MouseButton1Click:Connect(function()
    local inputKey = TextBox.Text
    if inputKey and inputKey ~= "" then
        pcall(function()
            local response = game:HttpGet(keyURL)
            if inputKey == response then
                validKey = inputKey
                ScreenGui:Destroy()
                -- Proceed to Show Main Hack Menu
                createHackMenu()
            else
                TextBox.Text = "Invalid key!"
            end
        end)
    end
end)

-- Main Hack Menu
local function createHackMenu()
    local menuGui = Instance.new("ScreenGui")
    menuGui.Name = "HackMenu"
    menuGui.ResetOnSpawn = false
    menuGui.Parent = game.CoreGui

    local menuFrame = Instance.new("Frame")
    menuFrame.Size = UDim2.new(0, 300, 0, 400)
    menuFrame.Position = UDim2.new(0.5, -150, 0.5, -200)
    menuFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    menuFrame.BorderSizePixel = 0
    menuFrame.Name = "MenuFrame"
    menuFrame.Parent = menuGui

    local title = Instance.new("TextLabel")
    title.Text = "Hack Menu"
    title.Size = UDim2.new(1, 0, 0, 30)
    title.BackgroundTransparency = 1
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.SourceSansBold
    title.TextSize = 22
    title.Parent = menuFrame

    -- Fly Button
    local flyButton = Instance.new("TextButton")
    flyButton.Text = "Fly"
    flyButton.Size = UDim2.new(0.8, 0, 0, 40)
    flyButton.Position = UDim2.new(0.1, 0, 0.15, 0)
    flyButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
    flyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    flyButton.Font = Enum.Font.SourceSansBold
    flyButton.TextSize = 20
    flyButton.Parent = menuFrame

    -- TP Aura Button
    local tpAuraButton = Instance.new("TextButton")
    tpAuraButton.Text = "TP Aura"
    tpAuraButton.Size = UDim2.new(0.8, 0, 0, 40)
    tpAuraButton.Position = UDim2.new(0.1, 0, 0.25, 0)
    tpAuraButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
    tpAuraButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    tpAuraButton.Font = Enum.Font.SourceSansBold
    tpAuraButton.TextSize = 20
    tpAuraButton.Parent = menuFrame

    -- Wallhack Button
    local wallhackButton = Instance.new("TextButton")
    wallhackButton.Text = "Wallhack"
    wallhackButton.Size = UDim2.new(0.8, 0, 0, 40)
    wallhackButton.Position = UDim2.new(0.1, 0, 0.35, 0)
    wallhackButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
    wallhackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    wallhackButton.Font = Enum.Font.SourceSansBold
    wallhackButton.TextSize = 20
    wallhackButton.Parent = menuFrame

    -- Blade Ball Button
    local bladeBallButton = Instance.new("TextButton")
    bladeBallButton.Text = "Blade Ball"
    bladeBallButton.Size = UDim2.new(0.8, 0, 0, 40)
    bladeBallButton.Position = UDim2.new(0.1, 0, 0.45, 0)
    bladeBallButton.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
    bladeBallButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    bladeBallButton.Font = Enum.Font.SourceSansBold
    bladeBallButton.TextSize = 20
    bladeBallButton.Parent = menuFrame

    -- Fly Function
    flyButton.MouseButton1Click:Connect(function()
        -- Activate fly functionality here
        print("Fly activated")
    end)

    -- TP Aura Function
    tpAuraButton.MouseButton1Click:Connect(function()
        -- Activate TP Aura functionality here
        print("TP Aura activated")
    end)

    -- Wallhack Function
    wallhackButton.MouseButton1Click:Connect(function()
        -- Activate Wallhack functionality here
        print("Wallhack activated")
    end)

    -- Blade Ball Function
    bladeBallButton.MouseButton1Click:Connect(function()
        -- Activate Blade Ball functionality here
        print("Blade Ball activated")
    end)
end
